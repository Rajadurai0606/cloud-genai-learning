----------- venv -----------

venv = Python virtual environment.

It creates an isolated environment for a project so different projects
can use different package versions without interfering with each other.

Example:

Project A -> FastAPI version X
Project B -> FastAPI version Y

Create virtual environment:

py -m venv .venv

Activate it in PowerShell:

.venv\Scripts\Activate.ps1


----------- pip -----------

pip is Python's package installer.

Example:

pip install fastapi[standard]

To save the packages currently installed in the project:

pip freeze > requirements.txt

Another developer can then install the same packages using:

pip install -r requirements.txt


----------- Python function syntax -----------

def get_message(name: str) -> str:
    return f"Hello {name}"


Python                          C#

def                             method/function declaration

name: str                       string name

-> str                          return type hint: string

f"Hello {name}"                 $"Hello {name}"

:                               starts the function/class block

Indentation                     defines the code block


Other examples:

def get_age(age: int) -> int:

def get_salary(salary: float) -> float:


----------- Class -----------

class Employee:
    def __init__(self, id: str, name: str, age: int, salary: float):
        self.id = id
        self.name = name
        self.age = age
        self.salary = salary


__init__ is similar to a constructor in C#.

self is similar to "this" in C#.


Create an object:

employee = Employee(
    "EMP001",
    "Raja",
    37,
    50000.0
)


Return an Employee object:

def get_employee(empid: str) -> Employee:
    employee = Employee(
        empid,
        "Raja",
        37,
        50000.0
    )

    return employee


----------- FastAPI -----------

FastAPI is a Python framework used to build REST APIs.

Create the application:

from fastapi import FastAPI

app = FastAPI()


We define routes/endpoints and the function below the route contains
the logic that handles the request.

Example:

@app.get("/health")
def health_check():
    return {
        "status": "healthy",
        "service": "new-learning-api"
    }


@app.get("/health")

means:

When an HTTP GET request comes to /health,
FastAPI should execute the function below it.


----------- Uvicorn -----------

Uvicorn is the web server that runs the FastAPI application.

Simple mental model:

Client / Browser
       |
       v
    Uvicorn
       |
       v
    FastAPI
       |
       v
Endpoint / Python function


----------- Swagger / docs -----------

FastAPI automatically generates API documentation.

Run the application and open:

http://127.0.0.1:8000/docs

Swagger UI allows us to:

- see available API endpoints
- see HTTP methods such as GET
- execute/test the API
- see request and response details

---------------------------------14-Sep-2026---------------------------------

pydantic
	installed -  pip install pydantic (ran in terminal)

	BaseModel

		Employee inherits from Pydantic BaseModel.

		This gives the model features such as:
		- request data validation
		- type conversion
		- field constraints
		- serialization
		- automatic schema generation

FastAPI uses this model to validate the incoming JSON request body.

	Created Employee class with Field constraints like minmum length, less than or equal and option field.
	
	I have executed "addemployee" endpoint through swagger with couple of request (negative/positive)


Also explored about model_dump (dictionary), model_dump_json (json format) which we can use later in out project

Day 2 - POST endpoint and Pydantic validation

Learned:
- BaseModel
- Field validation
- Optional fields
- POST request body
- FastAPI automatic validation
- 422 validation response
- model_dump()
- model_dump_json()

Tested:
- Valid employee request -> 200
- Invalid age -> 422
- Optional department omitted -> accepted


---------------------------------15-Sep-2026---------------------------------

Day 3 - Employee API - GET list + GET by ID

Python List
    Created an in-memory employee list:

    employees: list[Employee] = []

    Similar to List<Employee> in C#.

    append() is used to add an employee to the list.


Path Parameter

    Created an endpoint to get an employee using employee id:

    GET /employees/{id}

    The employee id from the URL is passed to the Python function.


Employee Lookup

    Loop through the employee list and compare employee id.

    If employee exists:
        return Employee

    If employee does not exist:
        raise HTTPException with 404 Not Found


HTTPException

    HTTPException should be raised, not returned.

    raise HTTPException(...)

    This stops normal processing and FastAPI returns an HTTP error response.


HTTP Status Codes used

    200 OK
        Request completed successfully.

    201 Created
        Employee was created successfully.

    404 Not Found
        Requested employee does not exist.

    409 Conflict
        Employee with the same id already exists.


GET All Employees

    GET /employees

    Returns the employees currently stored in the in-memory list.


In-Memory Storage

    employees list exists only in the running Python process memory.

    When Uvicorn/application is restarted, the list is initialized again:

    employees: list[Employee] = []

    Therefore all previously added employees are lost.

    Later we will need persistent storage/database when we want data
    to survive application restarts.


Tested through Swagger

    POST /employees
        New employee -> 201 Created

    POST /employees with same id
        Duplicate employee -> 409 Conflict

    GET /employees
        Returns employee list

    GET /employees/{id}
        Existing employee -> 200 OK

    GET /employees/{id}
        Unknown employee -> 404 Not Found

---------------------------------16-Sep-2026---------------------------------

Day 4 - Complete Employee CRUD

Implemented:

PUT /employees/{id}
    Updates an existing employee.
    Employee ID comes from the path parameter.
    Request body uses EmployeeBase and does not contain the ID.
    Returns 200 OK.
    Returns 404 if employee does not exist.

DELETE /employees/{id}
    Finds the employee using get_employee_or_404().
    Removes the employee from the in-memory list.
    Returns 204 No Content.
    Returns 404 if employee does not exist.


Employee Models

EmployeeBase
    Contains common employee input fields:
    name, age and department.

EmployeeModel(EmployeeBase)
    Inherits EmployeeBase and adds id.

This avoids accepting different employee IDs from:
    URL:  /employees/1
    Body: id = 2


ID Generation

Initially considered:
    len(employees) + 1

This can create duplicate IDs after an employee is deleted.

Instead, find the highest existing employee ID and add 1.

Example:
    Existing IDs: 1, 3
    New ID: 4


PUT vs POST vs DELETE

POST
    Creates a new resource.
    Using 201 Created.

PUT
    Updates/replaces an existing resource.
    Using 200 OK because the update happens immediately.

DELETE
    Removes a resource.
    Using 204 No Content when successful.


CRUD completed:

POST    /employees
GET     /employees
GET     /employees/{id}
PUT     /employees/{id}
DELETE  /employees/{id}

---------------------------------17-Sep-2026---------------------------------

Day 5 - Refactor FastAPI Project Structure

Goal:
    Split the single main.py application into separate modules/packages
    without changing the existing API behaviour.


Python Module

    A Python .py file is called a module.

    Examples:
        main.py
        employee.py
        health.py


Python Package

    A package is used to organize related Python modules into directories.

    Current structure:

        app/
            main.py

            models/
                __init__.py
                employee.py

            routers/
                __init__.py
                employee.py
                health.py
                land.py


__init__.py

    __init__.py is the package initialization file.

    For the current project, keep __init__.py empty.

    It can later be used for package initialization or to expose
    selected objects from the package, but this is not required now.

    Modern Python also supports some packages without __init__.py,
    but we do not need that concept for the current project.


Relative Imports

    . means current package.

    Example from app/main.py:

        from .routers import employee, health, land

    main.py is inside the app package.

    Therefore .routers means:
        app -> routers


    .. means go to the parent package.

    Example from app/routers/employee.py:

        from ..models.employee import EmployeeModel, EmployeeBase

    employee.py is inside:
        app -> routers

    .. moves back to:
        app

    then models.employee points to:
        app -> models -> employee.py


Import Error Learned

    Initially __init__.py contained:

        from employee import *

    Python tried to find a top-level module named employee and raised:

        ModuleNotFoundError: No module named 'employee'

    The wildcard imports were not required, so __init__.py files
    were left empty.


APIRouter

    Employee endpoints were moved out of main.py into an employee router.

    APIRouter groups related API endpoints.

    main.py registers the routers using:

        app.include_router(...)

    This keeps main.py focused on creating/configuring the FastAPI
    application rather than containing all endpoint implementations.


Uvicorn Command

    uvicorn app.main:app --reload

    Meaning:

        app       -> Python package
        main      -> main.py module
        app       -> FastAPI object created inside main.py
        --reload  -> restart development server when code changes


Result

    FastAPI application is now separated into:
        - application startup/configuration
        - routers/endpoints
        - Pydantic models

    Existing CRUD API behaviour still works after refactoring.

    ---------------------------------19-Sep-2026---------------------------------

Day 6 - Automated API Testing with pytest

pytest
    Python testing framework used to automatically discover and run tests.

    Naming convention:
        Test files   -> test_*.py
        Test methods -> test_*

    Run tests from project root:
        py -m pytest -v

    -m -> run pytest as a Python module
    -v -> verbose output showing individual test results


FastAPI TestClient
    TestClient is used as a client to invoke our FastAPI endpoints
    during automated testing.

    We pass our FastAPI app to TestClient.

    It allows tests to call GET, POST, PUT, DELETE etc. without
    manually starting Uvicorn.

    Normal execution:
        Browser/Swagger -> Uvicorn -> FastAPI -> Endpoint

    Automated test:
        pytest -> TestClient -> FastAPI -> Endpoint

    TestClient communicates with the FastAPI ASGI application
    directly inside the test process. It does not require a real
    Uvicorn server/network connection.


HTTPX
    HTTPX is a Python HTTP client library.

    FastAPI/Starlette TestClient is based on HTTPX, which provides
    the underlying HTTP client functionality.

    Simple understanding:
        Uvicorn    -> Server
        HTTPX      -> HTTP client library
        TestClient -> Client used to test our FastAPI application

    HTTPX and HTTP/2 are different:
        HTTPX  -> Python library
        HTTP/2 -> Version of HTTP protocol

    HTTP/2 is not required for our current learning.


Basic Test Approach
    Arrange -> prepare test data
    Act     -> invoke API
    Assert  -> verify expected result


What we tested
    GET /health
        -> 200

    POST /employees
        -> 201
        -> validate returned employee fields
        -> verify generated ID exists

    GET /employees
        -> 200
        -> verify response is a list

    GET /employees/{id}
        -> create employee
        -> capture generated ID
        -> retrieve same employee
        -> verify returned data

    GET unknown employee
        -> 404

    DELETE /employees/{id}
        -> create employee
        -> capture generated ID
        -> delete employee
        -> 204
        -> GET same employee -> 404


Important Testing Learnings

    1. Do not hard-code generated IDs in tests.
       Capture the ID returned by POST and use it for later requests.

    2. Tests should create/control the data they depend on rather than
       assuming an employee already exists.

    3. Do not validate only the HTTP status code when behaviour also
       matters.

       Example:
           DELETE -> 204
           GET same employee -> 404

       This confirms the employee was actually deleted.

    4. Automated tests give repeatable verification and reduce the need
       to manually test every scenario through Swagger.


Current Limitation
    Tests currently share the application's in-memory employee data,
    so tests can potentially affect each other.

    Test isolation and pytest fixtures will be learned later when needed.

---------------------------------21-Sep-2026---------------------------------

Day 7 - Dockerize FastAPI Application

Goal:
    Package and run the Employee FastAPI application inside Docker.


Docker
    Docker allows us to package an application together with its
    runtime and dependencies so it can run consistently in different
    environments.


Dockerfile
    Dockerfile contains the instructions Docker follows to build
    an image.

    Main instructions learned:

        FROM
            Defines the base image/runtime to start from.

        WORKDIR
            Sets the working directory inside the image/container.

        COPY
            Copies required files from our project into the image.

        RUN
            Executes commands while building the image.
            Example use: installing Python dependencies.

        CMD
            Defines the default command executed when the container starts.
            In our case, it starts Uvicorn/FastAPI.


Docker Image vs Container

    Image
        Packaged/read-only blueprint containing the application,
        Python runtime, dependencies and configuration needed to run it.

    Container
        A running instance of an image.

    Simple flow:

        Dockerfile
            ->
        docker build
            ->
        Image
            ->
        docker run
            ->
        Container


Building an Image

    docker build -t my-fastapi .

    docker build
        Builds a Docker image.

    -t my-fastapi
        Gives the image a name/tag.

    .
        Current directory is used as the Docker build context.


Docker Build Context

    Build context is the set of files/directories Docker can access
    while building the image.

    Using "." means the current project directory is the build context.


.dockerignore

    .dockerignore tells Docker which files/folders should NOT be
    included in the build context.

    We excluded unnecessary local files such as:

        .venv
        venv
        __pycache__
        *.pyc
        .pytest_cache
        Notes.txt
        README.md
        .gitignore

    This avoids sending unnecessary development/local files into
    the Docker build process.

    .gitignore and .dockerignore have different purposes:

        .gitignore
            -> controls what Git should not track.

        .dockerignore
            -> controls what Docker should not include in build context.


Running the Container

    docker run -p 8000:8000 my-fastapi

    This creates and starts a container from the my-fastapi image.


Port Mapping

    -p 8000:8000

    Format:

        HOST_PORT : CONTAINER_PORT

    In our case:

        Windows localhost:8000
                ->
        Container port 8000

    This allows us to access the FastAPI application running inside
    the container from the host machine.


Why Uvicorn Uses 0.0.0.0 Inside Docker

    Inside the container, Uvicorn must listen on 0.0.0.0 so the
    application can accept connections coming through the container's
    network interface.

    If it listens only on localhost/127.0.0.1 inside the container,
    it would only be reachable from inside that container.

    0.0.0.0 means listen on all available network interfaces.


Docker Image Layers

    Docker images are built using layers.

    The base Python image already contains multiple layers.
    Our Dockerfile adds additional layers on top.

    docker image history my-fastapi

    can be used to inspect image history/layers.

    Seeing <missing> for some IMAGE values in image history does not
    mean the build failed.


Commands Learned

    docker --version
        Check Docker installation.

    docker build -t my-fastapi .
        Build image.

    docker images
        List local images.

    docker run -p 8000:8000 my-fastapi
        Create and run container.

    docker image history my-fastapi
        View image build/layer history.


Important Learning

    Before Docker:

        Windows
            ->
        Local Python/.venv
            ->
        Uvicorn
            ->
        FastAPI


    With Docker:

        Windows
            ->
        Docker
            ->
        Container
            ->
        Python + Dependencies + Uvicorn + FastAPI


    The application is now packaged with the environment required
    to run it rather than depending directly on the local Python setup.


Development Flow So Far

    Code
        ->
    Automated Test
        ->
    Build Docker Image
        ->
    Run Container

    Later CI/CD will automate much of this process.


Current Limitation

    Employee data is still stored only in an in-memory Python list.

    Restarting/recreating the application/container will lose that data.

    Persistent storage/database will be handled later when required.


Day 7 Result

    Successfully:

        - Created Dockerfile
        - Created .dockerignore
        - Built my-fastapi Docker image
        - Verified the image using docker images
        - Inspected image history/layers
        - Started a container from the image
        - Mapped host port 8000 to container port 8000
        - Ran Uvicorn/FastAPI successfully inside Docker

    Day 7 - Completed.